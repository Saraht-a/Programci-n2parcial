console.log("Menu 1. Suma, 2. Resta, 3. Multiplicación, 4. División, 5. Salir"); let opcion = 4; // Simulando una opción seleccionada
let numero1
let numero2 switch (opcion)
{ 
    case 1:
console.log("Has seleccionado Suma"); numero1 = 10; // Simulando un número
numero2 = 5; // Simulando otro número
console.log("La suma de ${numero1} y ${numero2} es: + sumar(numero1, numero2)); sumar(numero1, numero2);
console.log("Has seleccionado Resta");

break;

case 2:

numero1 = 8; // Simulando un número

case 4:
numero2 = 5; // Simulando otro número
console.log('La resta de ${numero1} y ${numero2} es: " + restar (numero1, numero2));
break;
case 3:
console.log("Has seleccionado Multiplicación");
numero1 = 4; // Simulando un número
numero2 = 3; // Simulando otro número
console.log('La multiplicación de ${numero1} y ${numero2} es:` + multiplicar (numero1, numero2));
break;
case 4:
console.log("Has seleccionado División"); numero1 = 20; // Simulando un número numero2 = 4; // Simulando otro número
console.log('La división de ${numero1} y ${numero2} es: " + dividir(numero1, numero2));
break;

case 5:

console.log("Saliendo del programa...");

break;

default:

console.log("Opción no válida");

}

function sumar(a, b) {

return a + b;
}

function restar(a, b) {
}
return a - b;
function multiplicar(a, b) {
}
return a * b;
function dividir(a, b) {
if (b === 0) {

}

}
console.log("Error: División por cero no permitida");
return undefined;

//Suma con flecha function const sumaArrow = (a, b) => return a + b;
console.log("La suma es "+sumaArrow(5, 3)) //Suma con flecha function simplificada
const sumaArrowSimplificada = (a, b) => a+b; 
console.log("La suma es "+sumaArrowSimplificada (5, 3))


//Funciones
 function saludar(nombre='Alumn@") {


}

console.log("Hola, ${nombre}');
saludar("Helen");
saludar("Samuel");
saludar("Sara");
 saludar();

 //Suma
function sumar(a, b) { return a + b;
}
 console.log("La suma es "+sumar(5, 3))
16
17 function sumar1(a,b){

19
}
let x = a + b;
//console.log("La suma es "+(a + b));
console.log(sumar1(5, 3))
const suma = function(a, b) {
}
return a + b;
console.log("La suma es "+suma(5, 3))
//Suma con flecha
 function const sumaArrow = (a, b) => { 
 return a + b;
return a / b;